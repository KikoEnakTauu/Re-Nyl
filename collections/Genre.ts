import type { CollectionConfig } from "payload";

export const Genre: CollectionConfig = {
  slug: "Genre",
  admin: {
    useAsTitle: "name",
  },
  fields: [
    {
      name: "name",
      type: "text",
      required: true,
    },
    {
      name: "slug",
      type: "text",
      required: true,
      unique: true,
      index: true,
    },
  ],
};
